

## Plan: เพิ่ม Blur พื้นหลัง 80% เมื่อ Modal ปรากฏ

แก้ไขไฟล์ `src/components/AddTransactionFAB.tsx` บรรทัดที่ 128:

- เปลี่ยน `bg-background/60 backdrop-blur-sm` เป็น `bg-background/80 backdrop-blur-xl` เพื่อให้พื้นหลังเบลอหนักขึ้นและทึบ 80%

